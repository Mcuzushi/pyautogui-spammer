# PyAutoGUI Spammer Script

This is a simple Python script that automatically types a message multiple times using **PyAutoGUI**.

## 📌 Features
- Waits 5 seconds before starting (so you can focus the window).
- Types the text `"MK"` repeatedly.
- Presses
